"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.printDatasource = void 0;
const ensureDatabaseExists_1 = require("../utils/ensureDatabaseExists");
const chalk_1 = __importDefault(require("chalk"));
async function printDatasource(schemaPath) {
    const dbInfo = await ensureDatabaseExists_1.getDbInfo(schemaPath);
    if (dbInfo.dbType) {
        console.info(chalk_1.default.dim(`Datasource "${dbInfo.name}": ${dbInfo.dbType} ${dbInfo.schemaWord} "${dbInfo.dbName}"${dbInfo.schema ? `, schema "${dbInfo.schema}"` : ''} at "${dbInfo.dbLocation}"`));
    }
    else {
        console.info(chalk_1.default.dim(`Datasource "${dbInfo.name}"`));
    }
}
exports.printDatasource = printDatasource;
//# sourceMappingURL=printDatasource.js.map