"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.printFilesFromMigrationIds = exports.printFiles = void 0;
const indent_string_1 = __importDefault(require("indent-string"));
const printMigrationId_1 = require("./printMigrationId");
function printFiles(printPath, files) {
    const fileNames = Object.keys(files);
    const folders = printPath.split('/');
    const deepFolder = folders[1];
    return `\
${folders[0]}/
  └─ ${`${printMigrationId_1.printMigrationId(deepFolder)}/`}
${indent_string_1.default(fileNames.map((f) => `└─ ${f}`).join('\n'), 4)}`;
}
exports.printFiles = printFiles;
function printFilesFromMigrationIds(directory, migrationIds, files) {
    const fileNames = Object.keys(files);
    let message = `\
${directory}/`;
    migrationIds.forEach((migrationId) => {
        message += `\n  └─ ${printMigrationId_1.printMigrationId(migrationId)}/
${indent_string_1.default(fileNames.map((f) => `└─ ${f}`).join('\n'), 4)}`;
    });
    return message;
}
exports.printFilesFromMigrationIds = printFilesFromMigrationIds;
//# sourceMappingURL=printFiles.js.map