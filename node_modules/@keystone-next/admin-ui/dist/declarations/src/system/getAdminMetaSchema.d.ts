import { KeystoneConfig, BaseKeystone } from '@keystone-next/types';
import { GraphQLSchema } from 'graphql';
export declare function getAdminMetaSchema({ keystone, config, schema, }: {
    keystone: BaseKeystone;
    config: KeystoneConfig;
    schema: GraphQLSchema;
}): GraphQLSchema;
